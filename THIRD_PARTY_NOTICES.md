# Third-party notices

Runtime dependencies are declared in `daemon/package.json` and locked in
`daemon/package-lock.json`; dependencies are not vendored in this repository.
Their original licenses and notices remain applicable.

The repository also contains a local browser plugin source under
`daemon/laodeng/` attributed to `geekgeekrun`. The bundled upstream package
metadata does not declare a license. Confirm redistribution permission with
the upstream author before publishing this repository publicly or creating
binary releases that include that source. This project does not relicense
that component.

